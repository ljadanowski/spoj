#include <iostream>
using namespace std;

int znajdz(int tablica[], int rozmiar) {
  double srednia;
  int max, i, suma = 0;
  for(i=0; i<t; i++) suma += tablica[i];
  double srednia = (double) suma/rozmiar;
  return max;
}

int main()
{
  int t, i;
  cin >> t;
  int *tab = new int[t];
  
  for(i=0; i<t; i++) cin >> tab[i];
  
  cout << znajdz(tab, t) << endl;
}
